# Scenario

**Feature**: main behind origin → sync then land feature

```
main behind origin + ahead feature -> sync + rebased-and-merged land
```

Parent `with-remote` setup is sufficient; no leaf-specific mutations.
